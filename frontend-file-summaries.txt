{
  "frontend/src/App.tsx": "Main application component with TanStack Router setup including lobby, chatroom (with messageId search param validation), and hidden admin routes, with conditional header/footer rendering that hides both on admin pages.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations, restoring all previously exported members (`useCurrentUsername`, `useCurrentAvatar`, `uploadImage`, original `useDeleteChatroom`/`useUnpinVideo`/`useIncrementViewCount`/`useSendMessage` signatures) plus the new `useReportMessage` mutation and `useFlaggedMessages` query hooks.",
  "frontend/src/components/Header.tsx": "Application header with branding (MessageCircle icon in rounded-full bg-primary background), back navigation, clickable avatar icon that opens avatar picker dialog, and username button (outline variant) that opens inline editing mode with 15 character limit enforced via maxLength attribute and alphanumeric-only input validation, with light gray progress bar (bg-gray-200/dark:bg-gray-700) with blue fill (bg-primary) that only appears when the input is focused, showing proportional fill calculated outside JSX.",
  "frontend/src/components/ChatArea.tsx": "Chat interface component with collapsible header, fixed pinned video area, scrollable message container with auto-scroll, reply preview, message input, filtering of Creator messages without media, and scroll-to-target-message functionality. In the expanded header state on desktop, the message count, live viewer count, category tag, and chevron are now rendered beneath the description (centered) instead of to the right of the title/description.",
  "frontend/src/components/MessageBubble.tsx": "Individual message bubble component with avatar positioned at the top (self-start, h-10 w-10), slightly larger username and timestamp (text-sm), bg-white/40 reaction background for current user bubbles vs bg-gray-100 for others, quoted reply previews inside the bubble, always-visible icon-only action buttons (Reply, React via Popover, Share, Flag), and ExternalLinkDisclaimerModal using the correct onConfirm/targetUrl props matching its actual interface.",
  "frontend/src/components/MessageInput.tsx": "Enhanced message input component with pill-shaped textarea (bg-muted, no border), image/GIF picker tabs, circular SVG character counter ring inside the pill, and mic/send buttons placed outside the pill as flex-shrink-0 elements to prevent clipping. Uses correct `searchGiphy`/`fetchTrendingGiphy` imports from `@/lib/giphy` and `uploadImage` from `useQueries` for file uploads.",
  "frontend/src/components/ProfileSetupModal.tsx": "Modal dialog for first-time users to set up their profile name before accessing the chat.",
  "frontend/src/components/LoginPrompt.tsx": "No-op placeholder component that renders null to ensure no authentication UI is introduced, with inline documentation noting that authentication must not be added to this anonymous application.",
  "frontend/src/components/Footer.tsx": "Application footer with attribution and link to caffeine.ai.",
  "frontend/src/index.css": "Global styles with OKLCH color system, 100dvh height constraints for mobile support, iOS-specific CSS to enforce 16px font size on all input elements to prevent auto-zoom, voice message range input styling, and a new `.twitter-embed-container iframe` rule that forces the Twitter widget's injected iframe to a max-width of 300px.",
  "frontend/tailwind.config.js": "Tailwind CSS configuration with extended theme colors, border radius, and animation settings.",
  "frontend/src/pages/LobbyPage.tsx": "Lobby page with responsive grid layout, debounced search functionality, category filter badges displaying categories in lowercase, and algorithmic room sorting that prioritizes live rooms first, then sorts by view count descending, with creation date as tiebreaker, with all filtering and sorting performed client-side in-memory using the canonical ['chatrooms'] query.",
  "frontend/src/pages/ChatroomPage.tsx": "Chatroom page component with enhanced loading states, proper deep link support with messageId query parameter extraction from URL using TanStack Router's useSearch hook, improved error handling with back-to-lobby button, view count increment with user ID tracking on page load, and targetMessageId prop passed to ChatArea for scroll-to-message functionality.",
  "frontend/src/components/ChatroomCard.tsx": "Chatroom card component displaying enhanced dynamic thumbnails including YouTube, Twitch, and Twitter/X thumbnails with category badges now styled with px-2 py-0.5 padding to match LIVE tags and displaying category text in lowercase, with activeUserCount and viewCount display using Users/Eye icons for visual differentiation, text-only rooms now displaying a solid white speech bubble icon on a solid blue (primary) background, and timestamp now showing lastActivity (most recent message or reaction) instead of createdAt.",
  "frontend/src/components/CreateChatroomDialog.tsx": "Dialog form for creating chats with unified Video tab, Image tab, and Twitter tab with validation, live embed preview, category selection, now with character limits (65 for topic, 150 for description) enforced via maxLength attributes and light gray progress bars (bg-gray-200/dark:bg-gray-700) with blue fill (bg-primary) under each input that only appear when the respective input is focused, showing proportional fill calculated outside JSX.",
  "frontend/index.html": "HTML entry point with viewport configuration, cache-busting meta directives (Cache-Control/Pragma/Expires), and versioned module script URL (?v=203) to force fresh bundle fetches on production deployments.",
  "frontend/src/components/AvatarPickerDialog.tsx": "Dialog component for selecting user avatars with file upload (compressed via shared uploadImage), remove option, and Giphy GIF search with debounced input and trending GIFs display, with a fixed-height (h-64) scrollable GIF results area using ScrollArea and avatar fallback showing user initials derived from localStorage chatUsername.",
  "frontend/src/components/PinnedVideo.tsx": "Displays the pinned YouTube or Twitch video embed with a non-clickable Pin icon overlaid at the top-left corner and a clickable X (unpin) button overlaid at the top-right corner, both styled as small semi-transparent circular badges; the previous separate header row has been removed to reclaim vertical space.",
  "frontend/src/lib/formatters.ts": "Utility function for formatting numbers with compact notation (k, M, B) for displaying large counts in a user-friendly format.",
  "frontend/src/lib/videoUtils.ts": "Utility functions for extracting video IDs and generating thumbnail URLs, with enhanced URL detection supporting YouTube, Twitch, and Twitter/X URLs, plus a new detectMediaType helper function for automatic media type identification.",
  "frontend/src/components/VoiceMessagePlayer.tsx": "Voice message player component with Uiverse green-audio-player design, featuring circular play/pause button, progress slider with centered thumb, duration timer, and enhanced audio metadata loading that resets state on audioUrl changes and listens to durationchange events for reliable desktop duration display.",
  "frontend/src/lib/html2canvasLoader.ts": "Lazy loader for html2canvas library from CDN that dynamically injects the script only when needed, with timeout handling and typed function return.",
  "frontend/src/lib/twitterEmbedRenderer.ts": "Offscreen renderer for Twitter oEmbed HTML that creates isolated DOM containers for snapshotting, fetches oEmbed data, and provides cleanup functions.",
  "frontend/src/lib/twitterThumbnailCache.ts": "localStorage cache manager for Twitter thumbnails with stable key generation (prefers tweet ID), validation, expiration (7 days), and graceful error handling.",
  "frontend/src/lib/twitterThumbnail.ts": "Client-side Twitter thumbnail generator using html2canvas that fetches oEmbed HTML, renders offscreen, snapshots with html2canvas, caches results, and deduplicates concurrent requests.",
  "frontend/src/lib/twitterOEmbedPreview.ts": "Twitter oEmbed preview reconstruction utility that fetches Twitter oEmbed JSON (with omit_script=true) and extracts safe preview fields (author name, text snippet, optional image URL) without using html2canvas or DOM snapshotting, with in-flight request deduplication and caching integration.",
  "frontend/src/lib/twitterOEmbedPreviewCache.ts": "localStorage cache manager for Twitter oEmbed preview objects with stable key generation using tweet ID (via getTwitterPostId), schema validation, 7-day expiration, and graceful error handling.",
  "frontend/src/lib/config.ts": "Frontend configuration file containing the Giphy API key and result limit constants.",
  "frontend/src/hooks/useDebouncedValue.ts": "React hook that debounces a value with configurable delay to prevent excessive API calls during user input.",
  "frontend/src/lib/giphy.ts": "Client-side Giphy utility with search and trending endpoints that fetch GIF results from the Giphy API with error handling and structured result mapping.",
  "frontend/src/pages/AdminDeleteChatroomsPage.tsx": "Moderation page (renamed from \"Admin/Delete Chatrooms\") at `/admin/delete-chatrooms` with sessionStorage password gate, a new \"Flagged Messages\" section at the top showing flagged messages sorted by flag count descending with reasons and chatroom info via `FlaggedMessageCard`, and the existing chatrooms table with delete functionality.",
  "frontend/src/lib/imageCompression.ts": "Client-side image compression utility that downscales large images to max 1920px dimensions, encodes to JPEG at 85% quality while preserving EXIF orientation, and gracefully falls back to original file on any error.",
  "frontend/src/hooks/useForceFreshChatroomsOnActorReady.ts": "React hook that performs a one-time cleanup of legacy empty-filter cache entries and explicitly purges any persisted empty array for the canonical ['chatrooms'] query key before forcing a fresh refetch when the actor becomes ready, exposing an isRecovering signal so the lobby can show a loading state during the cache purge and refetch sequence.",
  "frontend/src/components/ExternalLinkDisclaimerModal.tsx": "Modal dialog warning users before navigating to external links, with `font-mono` applied to the displayed URL and `gap-4` spacing between the Cancel and Continue buttons."
}